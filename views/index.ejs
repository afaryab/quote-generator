<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quote Generator - Daily Inspiration</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="home-page">
  <main>
    <div class="home-quote-container">
      <div class="quote-display animate-slide-up">
        <div class="quote-text">
          <span class="quote-icon">"</span>
          <%= latest.quote %>
          <span class="quote-icon">"</span>
        </div>
        <div class="quote-meta">
          <div class="meta-item">
            <span class="meta-label">Theme:</span> <%= latest.theme %>
          </div>
          <div class="meta-item">
            <span class="meta-label">Tone:</span> <%= latest.tone %>
          </div>
          <div class="meta-item">
            <span class="meta-label">Audience:</span> <%= latest.audience %>
          </div>
          <div class="meta-item">
            <span class="meta-label">Generated:</span> <%= new Date(latest.timestamp).toLocaleString() %>
          </div>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
